<tr>
  <td><%= participant.user %></td>
  <td><%= participant.ideation_session %></td>
  <td><%= participant.nickname %></td>
  <td><%= participant.avatar_file_name %></td>
  <td><%= participant.active %></td>
  <td><%= participant.email %></td>
  <td>
    <%= link_to participant, method: :delete, data: { confirm: 'Are you sure?' } do %>        
      Delete
    <% end %>
  </td>
</tr>
    
