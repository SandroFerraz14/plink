<%= form_for([@ideation_session, @ideation_session.ideas.build], id_theme: id_theme) do |f| %>
  <% if @ideation_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ideation_session.errors.count, "error") %> prohibited this idea from being saved:</h2>
      <ul>
      <% @ideation_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= hidden_field_tag "id_theme",id_theme %>



<br>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :text, class: "form-control", required: true %>
  </div>
  <br>
  <div class="field">
    <select name="d" class="styled">
      <option value="c1">Yellow</option>
      <option value="c2">Option 2</option>
      <option value="c3">Option 3</option>
      <option value="c4">Option 3</option>
      <option value="c5">Option 3</option>
      <option value="c6">Option 3</option>
      <option value="c7">Option 3</option>

    </select>

    <%= f.label :color %><br>
    <%= f.text_field :color, class: "form-control"  %>
  </div>
  <br>
  
    <button class="btn btn-sm btn-primary  m-t-n-xs" type="submit"><strong>Create Idea</strong></button>
  


<% end %>
